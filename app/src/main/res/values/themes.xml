<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 🎨 ANA TEMA - Mistik Altın Lacivert                                 -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <style name="Theme.HadisimVar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/mistik_accent</item>
        <item name="colorPrimaryVariant">@color/mistik_accent_dark</item>
        <item name="colorOnPrimary">@color/mistik_primary_dark</item>
        <item name="colorSecondary">@color/mistik_accent</item>
        <item name="colorOnSecondary">@color/mistik_primary_dark</item>

        <item name="android:statusBarColor" tools:targetApi="l">@color/mistik_primary_dark</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/mistik_primary_dark</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>

        <item name="android:windowAnimationStyle">@style/WindowAnimationStyle</item>
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
        <item name="materialCardViewStyle">@style/Widget.App.CardView</item>
        <item name="floatingActionButtonStyle">@style/Widget.App.FAB</item>

        <item name="android:colorBackground">@color/mistik_background</item>
        <item name="colorSurface">@color/mistik_surface</item>
        <item name="colorOnSurface">@color/mistik_on_background</item>
    </style>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 🎬 PENCERE ANİMASYON                                               -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <style name="WindowAnimationStyle" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/slide_in_right</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_out_left</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_in_left</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_out_right</item>
    </style>

    <!-- Widget Stilleri -->
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <item name="cornerRadius">14dp</item>
        <item name="android:textAllCaps">false</item>
    </style>
    <style name="Widget.App.CardView" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">20dp</item>
        <item name="cardElevation">8dp</item>
    </style>
    <style name="Widget.App.FAB" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="elevation">8dp</item>
        <item name="borderWidth">0dp</item>
    </style>

    <!-- Splash - Mistik Gradient -->
    <style name="Theme.HadisimVar.Splash" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/bg_gradient_mistik</item>
        <item name="android:statusBarColor">@color/mistik_primary_dark</item>
        <item name="android:navigationBarColor">@color/mistik_primary_dark</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 💎 TEMA 1: SAF SAFİR (Klasik Modern)                               -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <style name="Theme.HadisimVar.Safir" parent="Theme.HadisimVar">
        <item name="colorPrimary">@color/safir_primary</item>
        <item name="colorPrimaryVariant">@color/safir_primary_dark</item>
        <item name="colorOnPrimary">@color/safir_on_primary</item>
        <item name="colorSecondary">@color/safir_accent</item>
        <item name="android:statusBarColor">@color/safir_primary_dark</item>
        <item name="android:navigationBarColor">@color/safir_primary_dark</item>
        <item name="android:windowBackground">@drawable/bg_gradient_safir</item>
        <item name="android:colorBackground">@color/safir_background</item>
        <item name="colorSurface">@color/safir_surface</item>
        <item name="colorOnSurface">@color/safir_on_background</item>
    </style>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 🌿 TEMA 2: MEDİNE YEŞİLİ (Geleneksel & Ruhani)                     -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <style name="Theme.HadisimVar.Medine" parent="Theme.HadisimVar">
        <item name="colorPrimary">@color/medine_primary</item>
        <item name="colorPrimaryVariant">@color/medine_primary_dark</item>
        <item name="colorOnPrimary">@color/medine_on_primary</item>
        <item name="colorSecondary">@color/medine_accent</item>
        <item name="android:statusBarColor">@color/medine_primary_dark</item>
        <item name="android:navigationBarColor">@color/medine_primary_dark</item>
        <item name="android:windowBackground">@drawable/bg_gradient_medine</item>
        <item name="android:colorBackground">@color/medine_background</item>
        <item name="colorSurface">@color/medine_surface</item>
        <item name="colorOnSurface">@color/medine_on_background</item>
    </style>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 🌙 TEMA 3: GECE OKUMASI (Dark Mode / Premium)                      -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <style name="Theme.HadisimVar.Gece" parent="Theme.HadisimVar">
        <item name="colorPrimary">@color/gece_accent</item>
        <item name="colorPrimaryVariant">@color/gece_primary_dark</item>
        <item name="colorOnPrimary">@color/gece_primary</item>
        <item name="colorSecondary">@color/gece_accent</item>
        <item name="android:statusBarColor">@color/gece_primary_dark</item>
        <item name="android:navigationBarColor">@color/gece_primary_dark</item>
        <item name="android:windowBackground">@drawable/bg_gradient_gece</item>
        <item name="android:colorBackground">@color/gece_background</item>
        <item name="colorSurface">@color/gece_surface</item>
        <item name="colorOnSurface">@color/gece_on_background</item>
    </style>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 🏜️ TEMA 4: SAHRA KUMLARI (Minimalist & Sıcak)                     -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <style name="Theme.HadisimVar.Sahra" parent="Theme.HadisimVar">
        <item name="colorPrimary">@color/sahra_primary</item>
        <item name="colorPrimaryVariant">@color/sahra_primary_dark</item>
        <item name="colorOnPrimary">@color/sahra_on_primary</item>
        <item name="colorSecondary">@color/sahra_accent</item>
        <item name="android:statusBarColor">@color/sahra_primary_dark</item>
        <item name="android:navigationBarColor">@color/sahra_primary_dark</item>
        <item name="android:windowBackground">@drawable/bg_gradient_sahra</item>
        <item name="android:colorBackground">@color/sahra_background</item>
        <item name="colorSurface">@color/sahra_surface</item>
        <item name="colorOnSurface">@color/sahra_on_background</item>
    </style>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 🏛️ TEMA 5: ASİL MERMER (Hafif & Ferah)                            -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <style name="Theme.HadisimVar.Mermer" parent="Theme.HadisimVar">
        <item name="colorPrimary">@color/mermer_primary</item>
        <item name="colorPrimaryVariant">@color/mermer_primary_dark</item>
        <item name="colorOnPrimary">@color/mermer_on_primary</item>
        <item name="colorSecondary">@color/mermer_accent</item>
        <item name="android:statusBarColor">@color/mermer_primary_dark</item>
        <item name="android:navigationBarColor">@color/mermer_primary_dark</item>
        <item name="android:windowBackground">@drawable/bg_gradient_mermer</item>
        <item name="android:colorBackground">@color/mermer_background</item>
        <item name="colorSurface">@color/mermer_surface</item>
        <item name="colorOnSurface">@color/mermer_on_background</item>
    </style>

    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <!-- 🌅 TEMA 6: GÜN BATIMI (Motivasyonel)                               -->
    <!-- ═══════════════════════════════════════════════════════════════════ -->
    <style name="Theme.HadisimVar.GunBatimi" parent="Theme.HadisimVar">
        <item name="colorPrimary">@color/gunbatimi_primary</item>
        <item name="colorPrimaryVariant">@color/gunbatimi_primary_dark</item>
        <item name="colorOnPrimary">@color/gunbatimi_on_primary</item>
        <item name="colorSecondary">@color/gunbatimi_accent</item>
        <item name="android:statusBarColor">@color/gunbatimi_primary_dark</item>
        <item name="android:navigationBarColor">@color/gunbatimi_primary_dark</item>
        <item name="android:windowBackground">@drawable/bg_gradient_gunbatimi</item>
        <item name="android:colorBackground">@color/gunbatimi_background</item>
        <item name="colorSurface">@color/gunbatimi_surface</item>
        <item name="colorOnSurface">@color/gunbatimi_on_background</item>
    </style>

</resources>
